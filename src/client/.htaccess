RewriteEngine On

# Bypass the SPA for API calls
RewriteRule ^erp/api/(.*)$ http://127.0.0.1:3002/erp/api/$1 [P,L]

# Otherwise serve SPA
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^erp/ /erp/index.html [L]
